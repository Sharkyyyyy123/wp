function isprime(n) {
    for (var i=2 ; i<n ; i++) {
        if (n%i === 0) {
            return false;
        }
    }
    return true;
}

let get = prompt("請輸入一個整數(判斷是否為質數) : ");
get = parseInt(get); 
console.log(get + "是質數嗎?\n答：" + isprime(get));
